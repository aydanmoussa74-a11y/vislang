{
  "vislang": "0.1",
  "id": "example",
  "name": "Example",
  "version": "0.1.0",
  "status": "draft",
  "binds": {
    "tokens": { "format": "none" },
    "roles": [{ "role": "action.primary", "required": true, "fallback": "#000000" }]
  },
  "invariants": [
    {
      "id": "INV-SEL-01",
      "statement": "selector_must_exist forbids empty selector.",
      "severity": "error",
      "applies_to": [{}],
      "predicate": { "kind": "selector_must_exist", "selector": {} }
    }
  ]
}
